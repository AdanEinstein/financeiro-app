import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#222",
        padding: 15,
    },
    total: {},
    totalN: {
        color: "#FA2508",
        fontWeight: "bold",
        fontSize: 20,
    },
    totalP: {
        color: "#88E30B",
        fontWeight: "bold",
        fontSize: 20,
    },
});

export default styles;
